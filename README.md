# LePruSim

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 22.0.0.

## Development server

To start a local development server together with the api, run:

```bash
npm run dev
```

Once the server is running, open your browser and navigate to `http://localhost:4200/`. The application will automatically reload whenever you modify any of the source files.

You can reach the api via `http://localhost:3000/api/catalogs` and `http://localhost:3000/api/questions/`.

Every time you run the API, a Script will automatically pull the Information of the Directory ./scripts/assets/questions/ where folders will be listed as 'Exams' and the JSON inside those folders will be our 'Topics'. That means, if a newly added JSON is in the right format and has the right path, the Script will automatically implement those Questions.


## Additional Resources

For more information on using the Angular CLI, including detailed command references, visit the [Angular CLI Overview and Command Reference](https://angular.dev/tools/cli) page.

---------------------------------------------------------------------------------

Dieses Projekt ist das Ergebnis einer Gruppenarbeit im Zuge einer Umschulung. Die Entwickelung dieses Projektes ist mit dem Abschluss des entsprechenden Moduls abgeschlossen und wird nicht Weitergeführt.
